I use a Sony ICD-PX370 Mono Digital Voice Recorder and this contains the
scripts that I use for transcription. The only requirement that needs to
happen outside of the script is running
`mkdir tmp`
`sudo mount /dev/sd<usually a1> tmp`
