I use a Sony ICD-PX370 Mono Digital Voice Recorder with this script 
after plugging the extendable usb in.

If the script fails, the recordings get moved to `tmp_recordings`. Just
go put them back in `REC_FILES` on the recorder to run the script
again.
